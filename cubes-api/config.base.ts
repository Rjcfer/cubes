import { Dialect } from "sequelize";

const config = {
  bd: {
    database: "cubes",
    host: "localhost",
    port: 3307,
    username: "root",
    password: "root",
  },
};
export default config;
