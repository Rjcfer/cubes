const config = {
  bd: {
    database: "cubes",
    host: "localhost",
    port: 3307,
    dialect: "mysql",
    username: "root",
    password: "root",
    models: ["./models"],
  },
};
export default config;
